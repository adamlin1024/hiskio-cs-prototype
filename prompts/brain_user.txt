# 用戶狀態
- 已登入:{is_logged_in}|稱呼:{user_name}|老客戶:{is_returning_customer}
- 對話輪數:{turn_count}|連續聽不懂:{consecutive_unclear_count}|離題累計:{off_topic_count}/{max_off_topic_count}
- 已提議過轉真人:{ticket_suggested}|用戶拒絕過轉真人:{user_declined_ticket}

# 既有意圖清單(index 對應 target_intent_index)
{intent_log_str}

# 最近對話
{recent_history}

# 用戶最新訊息
「{user_message}」
