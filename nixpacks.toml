[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python scripts/build_kb_index.py && python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"
